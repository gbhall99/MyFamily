{
  "name": "myfamily",
  "private": true,
  "version": "0.0.0",
  "description": "MyFamily — an AI/agentic family operating system. See docs/SPEC.md and docs/DESIGN_SPEC.md.",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b",
    "lint:tokens": "node tooling/token-lint.mjs",
    "verify": "pnpm run lint:tokens && pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
